# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code config. Local permissions and worktrees stay private, but
# launch.json is shared: CLAUDE.md tells every session to start the dev server
# from it, so a fresh clone needs it.
.claude/*
!.claude/launch.json
.claire/

# Supabase CLI local temp
supabase/.temp/

# Local scratch logs
mqlog.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Screenshot audits are kept locally, not versioned — 14 MB of PNGs in
# every clone for images nobody diffs.
screen-audit/
